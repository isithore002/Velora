{
  "name": "heartbeat-monitor",
  "description": "Check wallet balance every 5 minutes and notify on unexpected changes",
  "trigger": "cron",
  "chain": "base-sepolia",
  "chainId": "84532",
  "schedule": "*/5 * * * *",
  "steps": [
    {
      "action": "read_contract",
      "params": {
        "address": "{{WALLET_ADDRESS}}",
        "method": "balance",
        "args": []
      }
    },
    {
      "_note": "KNOWN BUG C-0001: webhook/send-webhook cannot resolve api.keeperhub.com from inside the execution environment. Using log action instead of webhook callback to avoid silent DNS failure.",
      "action": "log",
      "params": {
        "level": "info",
        "message": "Heartbeat: wallet={{WALLET_ADDRESS}} balance={{PREVIOUS_STEP_RESULT}} timestamp={{TIMESTAMP}}"
      }
    }
  ]
}
